import React from 'react';

function Company() {
  return (
    <body>
      <h1>Company</h1>
      <h2>Vite + React + TS (Hamburger + Responsive + Router)</h2>
    </body>
  );
}

export default Company;
