import React from 'react';

function NotFound() {
  return (
    <body>
      <h1>NOT FOUND</h1>
      <h2>Vite + React + TS (Hamburger + Responsive + Router)</h2>
      <input> </input>
    </body>
  );
}

export default NotFound;
