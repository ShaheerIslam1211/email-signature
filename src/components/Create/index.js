import React from 'react';

function Create() {
  return (
    <body>
      <h1>Create</h1>
      <h2>Vite + React + TS (Hamburger + Responsive + Router)</h2>
    </body>
  );
}

export default Create;
